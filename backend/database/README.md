# Database

This folder will hold database connection code and migrations when ClipForge moves from in-memory MVP storage to persistent storage.

Suggested first migration targets:

- `templates`
- `generation_jobs`
- `generated_assets`
